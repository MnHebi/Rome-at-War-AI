[agents]
# Economy mode is sequential by default. Two slots preserve an explicit user's
# ability to request a small independent pair without permitting broad fan-out.
max_concurrent_threads_per_session = 2

