{
  "objective": "one concrete outcome",
  "scope": ["included boundary"],
  "relevant_context": ["context node or stable artifact ID"],
  "known_facts": ["accepted fact with provenance"],
  "constraints": ["must preserve or must not do"],
  "files": ["repository-relative path"],
  "expected_output": "exact compact result needed by the parent",
  "validation": ["cheapest relevant deterministic check"],
  "stop_when": ["fact found, test passed, review complete, or bounded scope exhausted"]
}
