# T60 operand repair — source candidate, pending runtime

Implemented operand corrections only. No placement radius, clearance, retry,
danger, admission, boarding, gathering, Ctrl/stance or allocation policy change;
no new instrumentation; no deployment; installed files untouched. The
`gl-self-player-number` initializer issue is deliberately NOT bundled here.

## 1. Verified operand contracts (cached AIRef, `airef-reference-20260830.js`)

| Command | Operand roles | Repaired form |
|---|---|---|
| `up-can-build` | 1 = **EscrowGoalId**, 2 = typeOp, 3 = BuildingId | `(up-can-build gl-no-escrow-state c: X)` |
| `up-can-build-line` | 1 = **EscrowGoalId**, 2 = Point, 3 = typeOp, 4 = BuildingId | `(up-can-build-line gl-no-escrow-state <point> c: X)` |
| `up-build` | 1 = PlacementType, 2 = **EscrowGoalId**, 3 = typeOp, 4 = BuildingId | `(up-build <place> gl-no-escrow-state c: X)` |
| `up-build-line` | 1 = **Point**, 2 = **Point**, 3 = typeOp, 4 = BuildingId — **no escrow operand** | unchanged |
| `up-get-point-distance` | 1 = **Point**, 2 = **Point**, 3 = OutputGoalId | `(… gl-sy-memoryN-x gl-shipyard-x gl-sy-count)` |
| `up-train`/`up-can-train`/`up-research`/`up-can-research` | 1 = EscrowGoalId | already pass dynamic goals; unchanged |

Documentation shortcut vs runtime: `rawai-constants.per:474` documents `0` as
"without-escrow" for this operand role, while the 508 engine log reports
`Invalid goal used (0)` 86,277× for `up-can-build`, 3,001× for
`up-can-build-line`, and 24,490× for `up-get-point-distance`. That distinction is
preserved: the literal-0 shortcut is replaced by a real goal, and the
documented-shortcut probe (`tools/operand_smoke_probe.py`) is left untouched.

## 2. Dedicated immutable no-escrow goal

- `(defconst gl-no-escrow-state 396)` in `rawai-customconstants.per`, next to
  the other escrow-state goals. 396 is inside the documented extended-goal
  range 41-510, unused by every goal/point/technology name, not adjacent to any
  x/y point pair, and outside the file-trace storage (10000+/13000+).
- Initialized exactly once to the existing named value `without-escrow` in the
  one-shot escrow-initialization rule `rawai-init-goals.per:911` (loaded at
  `AI RAW.per:21`, before every consumer file).
- Never written afterwards: no `set-goal`/`up-modify-goal`/`up-get-*` site
  targets it (checked across all 108 root `.per` files).

## 3. Sites changed (140 operand replacements)

| Family | Sites | Where |
|---|---|---|
| `up-can-build` escrow operand | 68 | `rawai-homebase.per` 74, `rawai-military.per` 15, `rawai-wonder.per` 1 (55+12+1 outside/inside bridges per file) |
| `up-can-build-line` escrow operand | 18 | `rawai-military.per` 15, `rawai-specialplacement.per` 3 (generator), `rawai-fishing.per` 1, `rawai-homebase.per` 2 (incl. legacy `point-x`/`point3-x` points) |
| `up-build` escrow operand | 50 | `rawai-homebase.per` 47, `rawai-wonder.per` 1, `rawai-civ-kushans.per` 1, `rawai-tauntcommands.per` 1 |
| `up-get-point-distance` point operand | 4 | `rawai-specialplacement.per` 792/809/826/843 via `tools/generate_shipyard_placement.py` |

Dynamic escrow policy is preserved: the 5 `up-build … gl-buildingescrow-state`
rush sites and every `up-train`/`up-research`/`up-can-train`/`up-can-research`
site keep their goal operands. `up-build-line` sites were left alone.

## 4. Generation path (no hand-edited bridges)

1. Authoritative producers edited first (`tools/generate_shipyard_placement.py`
   for the 3 shipyard `up-can-build-line` sites and the 4 point operands).
2. Registered rules: the authoritative plain text lives in the command-boundary
   registry; the 63 affected site originals and their hashes were refreshed
   there (`.analysis/t60_apply_registry.py`), and the free text outside bridges
   was rewritten in the `.per` files (`.analysis/t60_apply_escrow.py`).
3. The emitted bridges, coverage identity, registry and support files were then
   regenerated by the project's own generator
   (`py -3.12 tools/generate_command_boundary.py --file-write --rescan`), after
   re-rendering the affected bridges with the project's `render_site` +
   `preserve_endings` (`--rescan` cannot validate a deliberately changed
   original before re-render).

Result: **579 rules / 817 commands unchanged**, site-id → file mapping
identical, only the intended operand text and two line offsets changed.
`OWNERSHIP-SOURCE-INVENTORY.md` regenerated with the supported
`tools/audit_ownership_source.py --write` (1,071 sites, no permission failures).

## 5. Measured budgets and validation

| Check | Result |
|---|---|
| `tools/generate_command_boundary.py` (no-write) | `PASS 579 rules 817 commands` (in sync) |
| `tools/validate_per.py` | no findings |
| Rule capacity (`validate_rule_capacity.py`) | 9,312/10,000, headroom 688 (unchanged) |
| String budget (`writer_trace.string_budget`) | 1,498/1,500 payload literals, unchanged |
| Goal budget | +1 extended goal (396); 41-510 still has 17 isolated free slots |
| Focused suites (16 modules, incl. new tests) | 288 run, OK, 2 pre-existing skips |
| New `tools/test_operand_repair.py` | 6 tests: allocation, one-shot init before consumers, immutability, no literal-zero escrow operand remains, `up-build-line` keeps its point signature, shipyard point operands |
| New `tools/test_command_boundary_log.py` | 7 tests: command contracts, deferred second, irrelevant remote list, genuine target mismatch, state/group/admission categories, recipients-unknown, ambiguity |
| Test expectations updated | 18 literal-zero assertions across 5 suites now expect the repaired operand; `tools/per_coastal_fixture.py` requires a goal operand (not the rejected literal) |

Status: **OPERAND REPAIR — PENDING RUNTIME.** The 508 recording cannot prove
the new operands remove the engine errors or improve shipyard placement; that
needs a fresh match with the same logging.
