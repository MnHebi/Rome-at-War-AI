# PER scripts contain legacy mixed line endings. Preserve them byte-for-byte
# and treat a carriage return as part of the line ending in whitespace checks.
*.per -text whitespace=trailing-space,cr-at-eol
